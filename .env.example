# SetPoint environment variables
#
# No runtime environment variables are defined yet.
# Add variable names here only when the architecture actually requires them.
# Never place real secrets in this file.
